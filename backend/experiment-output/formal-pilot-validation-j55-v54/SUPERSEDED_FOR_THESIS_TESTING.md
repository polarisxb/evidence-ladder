# Historical response-canary pilot: superseded notice

**Status: SUPERSEDED_FOR_THESIS_TESTING**

The earlier `formal-pilot-*` outputs are preserved for provenance and pipeline
regression only. They must not be cited as confirmatory evidence for
RE-TEST-NOT-RE-JUDGE.

The fixture used deterministic temperature-zero target behavior and instructed
canary echoing. Ground truth, judge-visible behavior, and B's canary evidence
therefore depended on substantially the same signal. A/A′ strong evidence was
also capped at E2 by construction. The run could validate orchestration,
accounting, temporal-block handling, and report generation, but it could not
estimate correction of naturally occurring judge errors.

The successor engineering validation is documented in:

- `stateful-calibration-phase-closure.md`
- `stateful-paid-gate-final-report.md`
- `stateful-paid-gate-analysis.{md,json,csv}`
- `stateful-paid-gate-rerun-analysis.{md,json,csv}`

Those successor runs are themselves deterministic calibration gates, not
formal thesis evidence. A separate model-driven protocol and new execution
approval are still required.
