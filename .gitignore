# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Environment
.env
backend/.env
mock_targets/*/.env

# Database / runtime data (sqlite db + WAL/SHM, pilot-run artifacts).
# CI recreates the directory via `mkdir -p data`, so nothing here is tracked.
backend/data/

# Build
frontend/dist/
backend/*.egg-info/
# Java/Maven build output for mock targets (compiled .class, maven-status, jars).
mock_targets/*/target/

# SQLite databases (recreated at runtime by seed scripts); never commit.
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
output/
.runtime-logs/
.pytest_cache/

# Tools
.playwright-cli/
.omx/
.worktrees/

# Cursor
# AI coding-assistant configuration — local-only, never published.
# .codex/ and .cursor/ hold agent skill definitions, rules and prompt design.
# They are developer tooling, not part of the released artifact, and they were
# also where the last hard-coded contest identifiers survived. Kept on disk so
# local workflow is unaffected; simply untracked.
.codex/
.cursor/

# Windsurf — local-only Superpowers skills (cloned from obra/superpowers)
# Skills are downloaded per machine; not committed to repo.
.windsurf/skills/

# Competition / submission archives — DO NOT publish
# These folders contain personal contest IDs, submitted reports, demo videos, etc.
# Excluded from the public repository to avoid accidental disclosure.
# Generic pattern — no hard-coded participant ID; also catches suffixed
# variants like "<id>-参赛总文件夹 - 省赛1备份/". No trailing slash: the archive
# also arrives as a single "<id>-参赛总文件夹.zip", which a directory-only
# pattern lets through.
*参赛总文件夹*
docs/内部参考.zip
# 国赛材料整理(2026-06-27):新工作区(给队友写材料) + 省赛旧稿归档 — 同样不发布
国赛材料编写/
_归档_省赛旧稿/

# Personal / private working notes
docs/private/
*.private.md
*.draft.md
notepad.md

# Competition / strategy working docs (internal) — keep local, NEVER publish
docs/内部参考/
docs/innovation_positioning.zh-CN.md
docs/future_innovations.zh-CN.md
docs/opening_report.zh-CN.md
docs/research/
docs/dev-notes/
docs/superpowers/

# Local agent transcripts and cached tool outputs
agent-tools/
.terminals/

# Local scratch — throwaway probe/patch scripts and temp notes (never commit)
.tmp_*
*_tmp.md

# Personal Cursor MCP config (contains local user paths)
# Includes backup files like mcp.json.bak-rename-<timestamp>.
.cursor/mcp.json
.cursor/mcp.json.*
# Personal Cursor agent rules (local-only behavior tuning).
.cursor/rules/bajie-mcp.mdc

# Personal grant / 大创 application drafts — contain student ID / advisor info.
# Keep locally only; do not publish.
docs/dachuang_report*.md

# Personal presentation / poster / 大创 promotion materials.
# Kept locally; do not publish to GitHub.
docs/ppt-materials/
docs/assets/tech-route-figure-prompt.md

# LaTeX build artifacts (paper preview compiles in docs/ppt-materials).
*.aux
*.out
*.toc
*.synctex.gz

# Academic paper PDFs — keep locally only; cite via BibTeX/links instead
# (PDFs from USENIX/NeurIPS/ICML/etc. typically retain copyright;
#  the .txt extracts under docs/papers/ are kept for in-repo citation.)
docs/papers/**/*.pdf
*.pdf
