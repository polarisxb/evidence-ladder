interface:
  display_name: "Verification Before Completion"
  short_description: "Verification discipline before claiming a task is complete."
  default_prompt: "Use $verification-before-completion to verify the build, tests, or fix before declaring success."

policy:
  allow_implicit_invocation: true
