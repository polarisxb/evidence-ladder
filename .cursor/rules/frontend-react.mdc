---
description: React前端开发规范
globs: frontend/**/*.{tsx,ts}
alwaysApply: false
---

# React 前端开发规范

## 组件规范

- 函数式组件 + TypeScript，不使用 class 组件
- Props 使用 interface 定义，命名为 `{ComponentName}Props`
- 导出使用 named export，不使用 default export

```typescript
// ✅
interface ScanCardProps {
  scan: ScanTask;
  onSelect: (id: string) => void;
}

export function ScanCard({ scan, onSelect }: ScanCardProps) {
  return <div>...</div>;
}
```

## 状态管理

- 局部状态用 useState / useReducer
- 服务端状态用 fetch + useState（或 React Query 如果引入）
- 全局状态通过 Context 传递（如主题、用户设置）

## API 调用

所有 API 调用封装在 `src/api/` 中:

```typescript
// src/api/scans.ts
export async function createScan(config: ScanConfig): Promise<ScanTask> {
  return request<ScanTask>("/scans", {
    method: "POST",
    body: JSON.stringify(config),
  });
}
```

## 样式

- 使用 TailwindCSS utility classes
- 深色主题为默认
- 风险等级颜色统一：critical=red, high=orange, medium=yellow, low=green
- 安全工具风格：monospace 字体用于数据展示，深色背景

## 文件命名

- 组件: PascalCase (`ScanCard.tsx`)
- 工具函数: camelCase (`formatDate.ts`)
- 类型文件: camelCase (`index.ts`)
- 常量: UPPER_SNAKE_CASE
