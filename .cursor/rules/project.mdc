---
description: AI安全测试平台项目核心规则 - 架构约定、编码规范、模块职责
alwaysApply: true
---

# AI Security Testing Platform - 项目规则

## 项目概述

本项目是一个面向大模型应用的自动化安全评估系统，通过自动化红队测试检测AI应用中的安全漏洞（Prompt注入、系统提示词泄露、越狱等），帮助开发者发现并修复AI安全风险。

## 技术栈

- **后端**: Python 3.11+ / FastAPI / SQLAlchemy (async) / SQLite (aiosqlite)
- **前端**: React 18 / TypeScript / Vite / TailwindCSS / Recharts
- **AI**: OpenAI API (GPT-4o for analysis, GPT-4o-mini for generation)
- **部署**: Docker / Docker Compose

## 项目结构

```
ai-security/
├── backend/           # Python FastAPI 后端
│   ├── app/
│   │   ├── main.py
│   │   ├── config.py
│   │   ├── database.py
│   │   ├── models/        # SQLAlchemy ORM 模型
│   │   ├── schemas/       # Pydantic 请求/响应模型
│   │   ├── api/           # 路由处理器
│   │   ├── services/      # 业务逻辑
│   │   ├── core/          # 核心工具
│   │   └── attack_templates/  # 攻击载荷数据 (JSON)
│   ├── tests/
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/          # React 前端
│   ├── src/
│   │   ├── api/           # API 客户端
│   │   ├── components/    # 可复用组件
│   │   ├── pages/         # 页面组件
│   │   ├── hooks/         # 自定义 Hooks
│   │   ├── types/         # TypeScript 类型
│   │   └── utils/         # 工具函数
│   ├── package.json
│   └── Dockerfile
├── docker-compose.yml
└── README.md
```

## 编码约定

### Python 后端
- 使用 Python type hints，所有函数必须有参数和返回值类型注解
- 使用 `async/await`，所有数据库操作和 API 调用必须是异步的
- Pydantic v2 用于所有数据验证和序列化
- 异常使用自定义 AppException，不直接 raise HTTPException（除非在路由层）
- 配置通过 pydantic-settings 管理，敏感信息放 `.env` 文件
- 日志使用 Python logging 模块，格式: `logger = logging.getLogger(__name__)`

### TypeScript 前端
- 严格 TypeScript，不使用 `any`
- 组件使用函数式组件 + Hooks
- API 调用封装在 `src/api/` 目录中
- 所有类型定义在 `src/types/` 中
- 使用 TailwindCSS 进行样式开发，不写自定义 CSS（除非必要）
- 深色主题为默认主题

### 通用规则
- 代码中使用英文注释和变量名
- API 接口文档和用户界面使用中英双语（优先中文）
- Git commit message 使用英文，遵循 conventional commits 格式
- 不在代码中硬编码 API key 或密码

## 核心模块职责

### 攻击引擎 (attack_engine.py)
- 管理攻击模板库
- 执行攻击序列（发送payload到目标AI）
- 收集目标AI的响应
- 支持单轮和多轮攻击

### AI分析器 (ai_analyzer.py)
- 调用 OpenAI API 分析目标响应
- 判断攻击是否成功
- 提取泄露的信息和证据
- 输出结构化的分析结果

### 风险评分器 (risk_scorer.py)
- 基于多维度评分: 可利用性、影响范围、可靠性、修复难度
- 映射到 OWASP LLM Top 10 框架
- 计算综合风险评分

### 报告生成器 (report_generator.py)
- 生成结构化安全报告
- 包含总体评分、漏洞列表、修复建议
- 支持 JSON 和 HTML 格式导出

### 靶场模块 (vulnerable_ai.py)
- 内置可配置防护级别的 AI 聊天机器人
- Level 1-4 防护级别，用于演示和测试
- 基于 OpenAI API 构建，通过不同的 system prompt 实现不同防护

## API 设计约定

- RESTful 风格：GET 查询, POST 创建, PUT 更新, DELETE 删除
- 路径前缀: `/api/v1/`
- 响应格式: `{"data": ..., "message": "ok"}` 或 `{"error": "...", "detail": "..."}`
- 分页: `?page=1&page_size=20`
- WebSocket: `/ws/scans/{task_id}` 用于实时进度推送

## 安全评估框架

所有安全测试结果按以下维度评估:
- **OWASP映射**: 关联到 LLM01-LLM10
- **风险等级**: CRITICAL / HIGH / MEDIUM / LOW / INFO
- **攻击成功率**: 成功次数 / 总测试次数
- **置信度**: AI分析器对判断的信心 (0.0-1.0)
